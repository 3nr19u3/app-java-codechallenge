server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: transaction-service

  main:
    web-application-type: reactive

  r2dbc:
    url: ${R2DBC_URL:r2dbc:postgresql://localhost:5433/app-java-codechallenge}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP:transaction-service}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: "*"

    topic:
      name: ${KAFKA_TOPIC_TRANSACTIONS:transactions_fraud}

logging:
  level:
    io.r2dbc.postgresql.QUERY: ${LOG_R2DBC_QUERY:INFO}
    io.r2dbc.postgresql.PARAM: ${LOG_R2DBC_PARAM:INFO}
